# 🔢 SW-TEST-ASSIGNMENT: Currency Word Converter (Max 3 Digits)

โปรเจกต์นี้คือโปรแกรมแปลงตัวเลขจำนวนเต็ม (สูงสุด 3 หลัก) ให้เป็นข้อความสกุลเงินภาษาอังกฤษ (English Currency Word) พร้อมด้วย User Interface (UI) โดยใช้ Python และ Tkinter และเน้นการใช้ **Stack** ใน Business Logic ตามข้อกำหนดของโจทย์ เพื่อให้ง่ายต่อการทำ Software Testing

## 🚀 วิธีการรันโปรแกรม

โปรแกรมนี้ใช้ Python 3 และ Tkinter (เป็นไลบรารีมาตรฐาน) ในการรัน

### Prerequisites

* ติดตั้ง Python 3 (เวอร์ชัน 3.6 ขึ้นไป)

### Execution

1.  บันทึกโค้ดทั้งหมดในไฟล์ `assignment-3.py`
2.  เปิด Terminal/Command Prompt ในโฟลเดอร์ของไฟล์
3.  รันคำสั่ง:

```bash
python assignment-3.py
